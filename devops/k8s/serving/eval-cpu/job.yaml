apiVersion: batch/v1
kind: Job
metadata:
  name: serving-eval-cpu
  namespace: mlops
  labels:
    app: serving-eval-cpu
    project: proj06
spec:
  ttlSecondsAfterFinished: 300
  template:
    metadata:
      labels:
        app: serving-eval-cpu
        project: proj06
    spec:
      restartPolicy: Never
      containers:
        - name: serving-eval-cpu
          image: ghcr.io/riyampatel2001/mlops-project-proj06/serving-eval-cpu:latest
          resources:
            requests:
              cpu: "1000m"
              memory: "2Gi"
            limits:
              cpu: "2000m"
              memory: "4Gi"
          env:
            - name: SERVING_ENDPOINT
              value: "http://transaction-classifier:8000"
            - name: MODEL_PATH
              value: "/models"
          volumeMounts:
            - name: model-storage
              mountPath: /models
      volumes:
        - name: model-storage
          persistentVolumeClaim:
            claimName: serving-models-pvc

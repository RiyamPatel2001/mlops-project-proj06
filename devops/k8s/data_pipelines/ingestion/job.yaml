apiVersion: batch/v1
kind: Job
metadata:
  name: ingestion-pipeline
  namespace: mlops
  labels:
    app: ingestion-pipeline
    project: proj06
spec:
  ttlSecondsAfterFinished: 300
  template:
    metadata:
      labels:
        app: ingestion-pipeline
        project: proj06
    spec:
      restartPolicy: Never
      containers:
        - name: ingestion-pipeline
          image: ghcr.io/riyampatel2001/mlops-project-proj06/ingestion-pipeline:latest
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          env:
            - name: DATA_OUTPUT_PATH
              value: "/data/processed"
          volumeMounts:
            - name: data-storage
              mountPath: /data
      volumes:
        - name: data-storage
          persistentVolumeClaim:
            claimName: data-pvc

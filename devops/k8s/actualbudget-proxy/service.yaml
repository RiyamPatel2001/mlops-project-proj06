apiVersion: v1
kind: Service
metadata:
  name: actualbudget-proxy
  namespace: mlops
  labels:
    app: actualbudget-proxy
    project: proj06
spec:
  type: NodePort
  selector:
    app: actualbudget-proxy
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30506

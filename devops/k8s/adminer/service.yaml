apiVersion: v1
kind: Service
metadata:
  name: adminer
  namespace: mlops
  labels:
    app: adminer
    project: proj06
spec:
  type: NodePort
  selector:
    app: adminer
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30081
